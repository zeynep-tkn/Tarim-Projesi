import 'package:flutter/material.dart';

class MalzemelerScreen extends StatelessWidget {
  const MalzemelerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Malzemeler Ekranı'));
  }
}
