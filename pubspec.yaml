name: tarim_proje
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"  # Dart SDK'yı güncelledik

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8  
  flutter_colorpicker: ^1.0.3

  # 🔥 Firebase paketlerini güncelledik
  firebase_core: ^2.32.0
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.15.4

  # ✅ Firebase Web için gerekli paketler
  firebase_auth_web: ^5.8.13
  js: ^0.7.1  # Firebase Web için gerekli (dart:js_interop desteği)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
